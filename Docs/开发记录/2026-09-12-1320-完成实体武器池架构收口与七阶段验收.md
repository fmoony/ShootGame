# 完成实体武器池架构收口与七阶段验收

- 日期：2026-09-12
- 计划提交说明：`回归：完成实体武器池架构收口与七阶段验收`
- 变更类型：文档 / 测试

## 目的

完成 [武器启动预配置与实体池简化重构方案](../执行计划/武器启动预配置与实体池简化重构方案.md) S5：
更新正式架构文档与 P1 前置，用一次七阶段完整回归重新验收武器与 Inventory 架构，
并审计自动化数量、网络成功标记、池容量与断线残留。

## 本提交完成内容

- `Docs/架构/Inventory与武器数据架构.md` 改为 WeaponId / WeaponActor / SlotIndex /
  CurrentWeaponActor 新模型，并记录正式验收基线；
- `Docs/执行计划/武器启动预配置与实体池简化重构方案.md` 标记 S1～S5 全部完成，
  补充 S5 验收证据；
- `Docs/执行计划/武器与Inventory正式架构实施计划.md` 把 RowName / InstanceId / 通用池
  描述标记为历史设计，并登记新验收结论；
- `Docs/执行计划/P1_LocalPredicted基础射击反馈执行计划.md` 更新前置状态与冻结 API；
- `Docs/执行计划/Shooter完整Demo最终路线规划.md` 更新当前阶段与证据链；
- `AGENTS.md` 补充 Inventory / Equipment / WeaponRuntimeSubsystem 结构导航与文档入口；
- 本提交不包含生产代码改动；生产与测试改动已在前序提交完成。

## 验证结果

- `Scripts/Tests/RunAll.ps1 -Port 17810`：七阶段全部 Passed；
- 报告：`Saved/Automation/Runs/20260912_130521/Summary.json`；
- Build：Passed（28.4s）；Automation：Passed，80 项成功、Failed=0、NotRun=0
  （报告 `Saved/Automation/Reports/20260912_130549_ShootGame/index.json`）；
- Standalone / Dedicated / Listen / Emulated / DisconnectCleanup：全部 Passed；
- 网络成功标记：Dedicated 2、Listen 1、Emulated 2；
- Emulated 日志包含 `PktLag set to 100` 与 `PktLoss set to 2`；
- Disconnect 日志：`DirtyPooled=0`、`Orphans=0`，`AUTOMATION_TEST_DISCONNECT_SUCCESS`
  与 `AUTOMATION_TEST_EQUIP_CLEANUP_SUCCESS` 均输出；
- 池容量审计：服务器启动日志 `WeaponRuntime initialized: Table=DT_WeaponData Buckets=4
  Authority=true`，四个正式 WeaponId 均按 InitialPoolSize 预热。

## 遇到的问题

无。

## 处理方式

无。

## 遗留项

- P1 Local Predicted 尚未实施，按计划等待用户批准；
- 池规模增长后的 Net Dormancy、自动缩容与 Projectile Pool 仍为非目标，待独立测量后再评估。
