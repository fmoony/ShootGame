# 完成动画分层阶段 A 验收

- 日期：2026-08-21
- 计划提交说明：`资产：完成第三人称动画分层阶段 A 验收并归档实施计划`
- 变更类型：资产 / 文档

## 目的

完成《第三人称动画分层实施计划》阶段 A 的最终验收与收口：Rifle / Pistol 正式 AnimBP 处于目标分层结构（DesiredState），视觉验收已由用户确认；将已完成的分层实施计划归档到《已完成计划》目录，为《第三人称瞄准表现同步实施计划》阶段 B 让出执行计划入口。本提交不混入任何 AimTarget 表现数据（按计划 §8.1）。

## 本提交完成内容

- 正式资产 `Content/Shooter/Animation/ThirdPerson/ABP_TP_Rifle.uasset`、`ABP_TP_Pistol.uasset`：最终分层结构（DesiredState）。
- 归档 `Docs/执行计划/第三人称动画分层实施计划.md` → `Docs/已完成计划/第三人称动画分层实施计划.md`，并修正归档文档内相对链接（指向《动画分层与射击表现扩展规划》改为 `../执行计划/` 前缀）。
- 修正《第三人称瞄准表现同步实施计划》中对分层计划的引用为归档后路径。
- 新增本开发记录。

### 最终拓扑与配置（与审计输出一致）

Rifle 与 Pistol 相同：

- AnimGraph：`Root.Result ← LBB.Pose`；`LBB.BlendPoses_0 ← Slot.Pose`；`Slot.Source ← RotationOffsetBlendSpace（含 AimOffset）.Pose`；`LBB.BasePose ← StateMachine.Pose`。
- LBB：BlendMode=0（BranchFilter）、Layers=1、LayerSetups=[Spine_01@1]（BlendDepth=1）、BlendWeights=[1.00]、MeshSpaceRotation=1、MeshSpaceScale=0、CurveOption=0、RootMotionByRootBone=1、LOD=-1。
- Slot：`Arms`（骨架 SK_Mannequin）。
- AimOffset：Rifle → `AO_Rifle`；Pistol → `AO_Pistol`（均为 Mesh Space Additive，参考姿势为各自 ADS Idle；X 轴无左右 Yaw 样本，首轮水平瞄准由身体承担）。
- FiringMontage：Rifle → `/Game/Shooter/Animation/FirstPerson/FP_Rifle_Shoot_Montage`；Pistol → `/Game/Characters/Mannequins/Anims/Pistol/MM_Pistol_Fire_Montage`；Slot `Arms`。

### 最终哈希（用户 8/21 21:20 在编辑器中重新保存后的验收字节）

- `ABP_TP_Rifle.uasset` MD5 = `D0F40F6D1C230233BBC74FC0024DCA55`（418299 字节）
- `ABP_TP_Pistol.uasset` MD5 = `245828624307E3B797A88A89720C924F`（416628 字节）

## 验证结果

- 只读 Audit（本轮，基于最终字节）：Rifle `STATE=DesiredState`、Pistol `STATE=DesiredState`，拓扑与 LBB 配置与上一致（`Saved/Automation/DevTools/b0_audit_rifle.txt`、`b0_audit_pistol.txt`，命令台退出码 0）。
- 幂等 Apply（本轮，基于最终字节）：双资产均 `ANIM_LAYER_APPLY_SKIP_DESIRED` + `ANIM_LAYER_APPLY_OK`，退出码 0，Apply 前后 MD5 不变（`b0_idempotent_rifle.log`、`b0_idempotent_pistol.log`）。
- 完整自动化：`Scripts/Tests/RunAll.ps1` 七阶段回归（2026-08-21 20:14 运行）全部 Passed，Automation 50/50 通过，Listen / Dedicated 双客户端会话通过（`Saved/Automation/Runs/20260821_201421/Summary.json`）。
- 双客户端视觉验收：Rifle 由用户于 8/21 19:46 正式 Apply 流程验收；Pistol 最终视觉验收由用户于 8/21 确认完成。
- 正式 Apply 历史：Rifle 2026-08-21 19:46（COMPILE/SAVE/POSTSAVE/APPLY 四 OK）；Pistol 2026-08-21 20:13（同样四 OK，退出码 0）。执行前原版备份位于 `Saved/Automation/DevTools/backup/*.uasset.bak`。

## 遇到的问题

- 用户于 21:20 在 UnrealEditor 中重新保存 Rifle/Pistol，磁盘字节与 MD5 与上一轮验收时不同（结构未变）。若直接沿用旧哈希提交，会与磁盘实际状态不一致。

## 处理方式

- 等待用户关闭编辑器后，重新执行只读 Audit 与幂等 Apply，确认结构仍为 DesiredState、MD5 不再变化后，以最终字节作为提交内容并记录新哈希。

## 遗留项

- 一次性 AnimBP 迁移工具的移除（B0 继续）：`Source/ShootGameEditor/` 模块源码与 `Scripts/DevTools` 探针从未进入 Git，按计划 §8.2 仅做工作区清理、不新增清理提交；随后重新生成 VS 工程文件并提示用户点击“全部重新加载”，命令行 UBT 重建后执行七阶段回归。
- `AGENTS.md` 与《动画分层与射击表现扩展规划》的导航/链接更新由用户维护（本提交未改动用户文件）。
- `Saved/Automation/DevTools` 运行产物是否本地清理由用户决定。
- 下一阶段按《第三人称瞄准表现同步实施计划》进入 B1：冻结可测量基线。
