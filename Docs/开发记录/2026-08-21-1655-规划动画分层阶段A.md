# 规划动画分层阶段 A

- 日期：2026-08-21
- 计划提交说明：`Docs：规划第三人称动画分层阶段 A 并收口导航`
- 变更类型：文档 / 构建

## 目的

为《动画分层与射击表现扩展规划》的“阶段 A：建立动画分层基线”建立可直接执行的入口文档，收口 AGENTS.md 与自动化测试说明的导航，并补上 Visual Studio 工程文件同步的规范与脚本，保证后续修改动画蓝图资产时不依赖人工在 VS 中重载工程。

## 本提交完成内容

- 新增《动画分层与射击表现扩展规划》：描述阶段 A～F 的表现路线与系统边界，作为父规划。
- 新增《第三人称动画分层实施计划》：阶段 A 的完整执行依据，包含基线审计、Rifle / Pistol AnimGraph 调整步骤、验证矩阵、提交拆分与完成定义。
- 删除已被上述两份文档取代的《GAS、动画分层与客户端预测扩展计划》，并更新《GAS接入前架构整理执行方案》中指向旧计划的引用为历史措辞。
- 更新 `AGENTS.md` 文档导航，指向新规划与实施计划，并新增“Visual Studio 项目文件同步”协作约定。
- 新增《Visual Studio 项目同步》说明文档与 `Scripts/Development/RefreshVisualStudioFiles.ps1` 刷新脚本。
- 更新《自动化测试》文档，在推荐执行顺序中加入 VS 工程文件刷新步骤。

## 验证结果

- 本次仅修改文档与脚本，未改动生产代码、蓝图或测试。
- `RefreshVisualStudioFiles.ps1` 为既有约定的固化，本提交未实际运行（源码结构本轮无变化，按文档约定跳过）；脚本仅调用 `UnrealBuildTool -ProjectFiles`，参数与既有文档一致。
- 未运行 Unreal 自动化；不涉及资产或网络行为。

## 遇到的问题

无。

## 处理方式

无。

## 遗留项

- 旧计划删除后，历史提交中的链接保持不变，不追溯改写。
- `Docs/Agent自动化验证操作手册.md` 存在 UTF-8 编码损坏（多处多字节字符末字节被替换为 `?`），属于既有问题，本提交不处理，读取时按损坏文本理解；后续可单独修复。
