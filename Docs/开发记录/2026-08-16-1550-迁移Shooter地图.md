# 迁移 Shooter 地图

- 日期：2026-08-16
- 计划提交说明：`资产：迁移 Shooter 地图`
- 变更类型：资产 + 配置

## 目的

执行 [Shooter Content Browser 资产目录整理执行方案](../执行计划/Shooter_Content_Browser_资产目录整理执行方案.md) 阶段 4：把 Shooter 两张地图迁移到 `/Game/Shooter/Maps/`，并同步所有地图相关硬编码路径，完成 `/Game/Variant_Shooter` 运行路径的收束。

## 本提交完成内容

- 迁移两张地图到 `/Game/Shooter/Maps/`：
  - `Lvl_Shooter`（World Partition 地图）→ `/Game/Shooter/Maps/Lvl_Shooter`
  - `Lvl_Test`（普通地图）→ `/Game/Shooter/Maps/Lvl_Test`
- World Partition 外部 Actor/Object 随地图迁移（132 个 ExternalActors + 10 个 ExternalObjects），未手工整理，由 UE 地图系统管理。
- 同步地图硬编码路径：
  - `Config/DefaultEngine.ini`：`EditorStartupMap`、`GameDefaultMap`、`ServerDefaultMap` → `/Game/Shooter/Maps/Lvl_Shooter.Lvl_Shooter`
  - `Config/DefaultEditor.ini`：`SimpleMapName` → `/Game/Shooter/Maps/Lvl_Shooter`
  - `Config/DefaultEditorPerProjectUserSettings.ini`：内容浏览器默认目录 → `/Game/Shooter`
  - `Scripts/Tests/RunAll.ps1`、`RunStandaloneSmoke.ps1`：`MapPath` → `/Game/Shooter/Maps/Lvl_Shooter`

## 验证结果

- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17794`，七阶段（Build / Automation / Standalone / DedicatedNetwork / ListenNetwork / EmulatedNetwork / DisconnectCleanup）全部 Passed。
- 实际结果文件：`Saved/Automation/Runs/20260816_154534/Summary.json`，顶层 `status: Passed`、`error: null`。
- Standalone、Dedicated、Listen 均从新路径 `/Game/Shooter/Maps/Lvl_Shooter` 正常加载地图，World Partition 正常，网络主闭环无回退。

## 遇到的问题

- 首次尝试用 Python `rename_asset` 迁移地图失败：World Partition 地图重命名时 UE 弹出「Continue with rename?」确认框，无头命令模式无法回答，返回失败。

## 处理方式

- 改由用户在 Unreal 编辑器内手动拖拽迁移地图并确认弹框，Fix Up Redirectors 后完成。

## 遗留项

- `/Game/Variant_Shooter` 下仍残留空目录结构（Anims/Blueprints/FX/Input/UI），留待阶段 5 清理。
- 全仓库扫描确认 `/Game/Variant_Shooter` 文本引用已清零（仅历史文档/开发记录保留历史原文）。
