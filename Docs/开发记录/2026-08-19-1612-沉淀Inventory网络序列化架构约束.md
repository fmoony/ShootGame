# 沉淀 Inventory 网络序列化架构约束

- 日期：2026-08-19
- 计划提交说明：`Docs：沉淀 Inventory 网络序列化架构约束`
- 变更类型：文档 / 注释

## 目的

对已完成并验证通过的 Inventory 2A（提交 `0c36702`）做长期架构约束沉淀，避免后续开发：

- 误以为 `FPrimaryAssetId DefinitionId` 已由默认 UPROPERTY 反射复制；
- 在 `FShooterWeaponInstanceData` 新增网络字段后只加 UPROPERTY、不更新 `NetSerialize`；
- 误读 `SetDeltaSerializationEnabled(false)` 为“关闭整个 FastArray Delta”。

## 本提交完成内容

- 新增 `Docs/架构/Inventory与武器数据架构.md`：
  - 记录 Inventory / WeaponInstance / WeaponActor 长期职责边界；
  - 记录 OwnerOnly 网络可见性；
  - 记录 FastArray 原生机制与 Item Payload 定制边界；
  - 准确记录 `FPrimaryAssetId` 的 UHT 反射约束；
  - 记录 `WithNetSerializer` 与关闭 Item 内部 Struct Delta 的原因；
  - 记录新增网络字段必须同步维护 `NetSerialize` 的 IMPORTANT 约束；
  - 明确 2B / 2C / 2D 尚未实现。
- 修改 `Docs/代码规范.md`：
  - 新增“自定义网络序列化”通用规范；
  - 只保留通用规则，并链接到 Inventory 架构文档。
- 修改 `Source/ShootGame/Inventory/ShooterInventoryTypes.h` 的注释：
  - 在 `FShooterWeaponInstanceEntry::NetSerialize` 前增加醒目的完整 Payload 维护约束；
  - 精确改写 `DefinitionId` 与 Struct Delta 注释；
  - 在 `WithNetSerializer` trait 旁补充当前必要性说明。
- 未修改任何运行逻辑、数据结构、网络协议、复制条件或测试逻辑。

## 验证结果

- `git diff --check`：通过，无 whitespace 错误。
- Markdown 检查：`Docs/架构/` 文件存在，标题与相对链接目标存在；`Docs/代码规范.md` 中的架构文档链接路径正确。
- `Source/ShootGame/Inventory/ShooterInventoryTypes.h` diff 检查：所有变更均为注释行，未改动任何 C++ 声明、调用或表达式。
- `Scripts/Tests/BuildEditor.ps1`：通过。
- 按任务要求，本次未运行 Dedicated / Listen / RunAll；注释与文档变更不改变网络行为。

## 遇到的问题

- 需要避免把新架构文档链接加入 `AGENTS.md`：该文件当前存在与本任务无关的未暂存修改，按工作区保护要求不触碰、不暂存。

## 处理方式

- 文档链接通过 `Docs/代码规范.md` 的“自定义网络序列化”章节指向 `Docs/架构/Inventory与武器数据架构.md`，不修改 `AGENTS.md`。
- 其余任务无异常，按计划只修改允许的文档、规范与注释。

## 遗留项

- 未进入 Inventory 2B。
- 未修改 Inventory 行为、网络协议、数据结构与测试。
- `FPrimaryAssetId DefinitionId` 仍保持当前非反射字段 + 边界 `NetSerialize` 方案；未来如需大量反射 / Blueprint / SaveGame 直读，再评估可反射包装类型。
- `AGENTS.md` 及 `ShooterGameMode.h`、`ShooterPlayerController.h/.cpp`、`ShooterWeapon.h/.cpp` 等无关工作区修改仍保留，本次未触碰、未暂存、未提交。
