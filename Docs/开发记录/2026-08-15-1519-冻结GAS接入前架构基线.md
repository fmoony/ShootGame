# 冻结 GAS 接入前架构基线

- 日期：2026-08-15
- 计划提交说明：`文档：冻结 GAS 接入前架构基线`
- 变更类型：文档

## 目的

执行 [GAS 接入前架构整理执行方案](GAS接入前架构整理执行方案.md) 阶段 5：同步项目文档，使源码结构、类去留结论与目录入口与实际代码一致，冻结 GAS 接入前的架构基线。

## 本提交完成内容

- 更新根目录 `AGENTS.md` 的源码结构：`Variant_Shooter/` 与根目录旧模板类替换为 `GameFramework` / `Characters` / `AI` / `Weapons` / `UI` / `Tests` 新目录结构。
- 更新 [FirstPerson 清理与架构审计](FirstPerson清理与架构审计.md) 的根目录 C++ 类去留最终结果：`AShootGameCharacter`、`AShootGamePlayerController`、`AShootGameCameraManager`、`AShootGameGameMode` 均标记为已删除，`AShooterCharacter`/`AShooterNPC` 直接继承 `ACharacter`；同步更新配置残留与「暂不执行」说明。
- 更新 [GAS、动画分层与客户端预测扩展计划](GAS动画分层与预测扩展计划.md) 的建议目录入口：`Source/ShootGame/Variant_Shooter/` 改为 `Source/ShootGame/` 下的 `AbilitySystem` / `Animation` / `Inventory` / `Characters` / `GameFramework`。
- 更新 [代码规范](代码规范.md) 中失效的示例类名 `AShootGameCharacter` → `AShooterCharacter`。

## 验证结果

- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17794`，七阶段（Build / Automation / Standalone / DedicatedNetwork / ListenNetwork / EmulatedNetwork / DisconnectCleanup）全部 Passed。
- 实际结果文件：`Saved/Automation/Runs/20260815_151654/Summary.json`，顶层 `status: Passed`、`error: null`。
- CodeGraph 确认旧根模板类 `AShootGameCharacter`、`AShootGamePlayerController`、`AShootGameGameMode`、`AShootGameCameraManager` 已无定义；`AI/ShooterAIController` 等可从新路径解析。
- `git diff --check` 无错误。

## 遇到的问题

无。

## 处理方式

无。

## 遗留项

- `/Game/Variant_Shooter` 仍为二进制资产路径（方案明确不在此计划内批量改名，留作独立资产迁移任务）。
- `Config/DefaultEditor.ini`、`CtrlRig_FPWarp.uasset` 为用户/编辑器侧改动，未纳入本提交。

## 计划完成说明

本提交为 [GAS 接入前架构整理执行方案](GAS接入前架构整理执行方案.md) 的最后一个阶段（阶段 5）。至此：Shooter 为唯一默认玩法入口、根目录旧模板框架类全部移除、玩家与 NPC 不再共享错误基类、`Source/ShootGame/Variant_Shooter/` 已移除、旧模块 `TP_FirstPerson` 引用与重定向已清空、七阶段自动化全绿，达到 GAS 接入前的架构基线。尚未启用 GAS 插件，也未创建 ASC、AttributeSet、GameplayAbility 或预测实现。
