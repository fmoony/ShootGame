# 由 Shooter 接管框架入口

- 日期：2026-08-15
- 计划提交说明：`架构：由 Shooter 接管框架入口`
- 变更类型：生产代码

## 目的

执行 [GAS 接入前架构整理执行方案](GAS接入前架构整理执行方案.md) 阶段 1：由 Shooter 接管框架入口，移除根目录残留的旧模板框架类，并把摄像机俯仰限制迁移到 Shooter 自己的摄像机管理器，作为 GAS 接入前的架构收束。

## 本提交完成内容

- 新增 `AShooterCameraManager`（`Source/ShootGame/Variant_Shooter/ShooterCameraManager.h/.cpp`），迁移原模板的俯仰限制 `ViewPitchMin = -70.0f`、`ViewPitchMax = 80.0f`。
- `AShooterPlayerController` 新增构造函数，设置 `PlayerCameraManagerClass = AShooterCameraManager::StaticClass()`，使 Shooter 实际使用带俯仰限制的摄像机管理器。
- 删除根目录旧模板类（无任何活跃 C++ 引用者，经 grep 确认）：
  - `ShootGameGameMode.h/.cpp`
  - `ShootGamePlayerController.h/.cpp`
  - `ShootGameCameraManager.h/.cpp`
- 确认 `BP_ShooterGameMode` 父类已是 `/Script/ShootGame.ShooterGameMode`（正式 Shooter 类），默认地图与默认 GameMode 均指向 Shooter 玩法。

## 验证结果

- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\BuildEditor.ps1`，删除旧类后 UHT 重新生成反射、编译链接成功，`Result: Succeeded`。
- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17794`，七阶段（Build / Automation / Standalone / DedicatedNetwork / ListenNetwork / EmulatedNetwork / DisconnectCleanup）全部 Passed。
- 实际结果文件：`Saved/Automation/Runs/20260815_135049/Summary.json`，顶层 `status: Passed`、`error: null`。
- Standalone 与双客户端网络测试正常加载资产，证明正式资产不依赖已删除的旧类。

## 遇到的问题

- 阶段 1 执行期间 Unreal 编辑器已关闭，只读蓝图 MCP 断连，无法实时确认 `BP_ShooterPlayerController` 的父类。

## 处理方式

- 通过完整自动化测试间接验证：Standalone 阶段与 Dedicated/Listen/Emulated/Disconnect 网络阶段均成功加载并驱动 PlayerController 链（HUD、计分、复活），证明 `BP_ShooterPlayerController` 未引用已删除的旧类。

## 遗留项

- `BP_ShooterPlayerController` 父类未通过 MCP 直接确认（编辑器关闭），仅经自动化测试间接验证；建议下次开启编辑器时在属性面板确认其父类为 `ShooterPlayerController`。
- `DefaultEngine.ini` 中 `TP_FirstPersonPlayerController` / `TP_FirstPersonGameMode` / `TP_FirstPersonCameraManager` 三个类重定向现在指向已删除的类，按方案留待阶段 4 统一收束，不得提前删除。
