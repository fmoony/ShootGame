# 修正反向表现修复测试的公共入口依赖

- 日期：2026-08-29
- 计划提交说明：`测试：修正反向表现修复测试的公共入口依赖`
- 变更类型：测试

## 目的

上一提交新增的 `NoReversePresentationRepair` 测试调用了工作区未提交测试壳方法 `CallUpdateShooterAnimationDataForTest`，干净检出上一提交时会编译失败。本提交把测试改为直接调用 UAnimInstance 公共入口 `NativeUpdateAnimation`，保证测试只依赖已提交代码。

## 本提交完成内容

- `ShooterIKBindingAutomationTests.cpp`：`NoReversePresentationRepair` 中 `CallUpdateShooterAnimationDataForTest` → `Harness->NativeUpdateAnimation(1.0f / 60.0f)`。
- 不改动生产代码，不处理工作区未提交的 `CallUpdateShooterAnimationDataForTest` 测试壳。

## 验证结果

- 干净 HEAD 场景的编译验证在最终 BuildEditor / 全量 Automation 中覆盖。
- 工作树当前（含用户未提交改动）BuildEditor 与 `ShootGame.Aim.Binding` 已通过。

## 遇到的问题

- 提交后复核发现测试对用户工作区 helper 的隐性依赖。

## 处理方式

- 改用 UAnimInstance 公共 API，并生成仅含该行的 HEAD 基线补丁暂存，避免混入用户 WIP。

## 遗留项

- 无。
