# 删除网络实验输入资产

- 日期：2026-08-15
- 计划提交说明：`清理：删除网络实验输入资产`
- 变更类型：资产 / 文档

## 目的

完成上一提交移除 NetCounter、RPC Matrix 与所有权实验代码后留下的资产侧清理，避免正式 Shooter 输入映射和动画支持蓝图继续保存已经无效的实验 Input Action 引用。

## 本提交完成内容

- 从 `/Game/Input/IMC_Default` 删除 `IA_NetCounterTest`、`IA_NetCounterLocalTest`、`IA_OwnerShipTest` 三个映射项。
- 强制重存 `/Game/Variant_Shooter/Blueprints/AnimationSupport/BP_FirstPersonCharacter`，清除已被 C++ 删除的 `NetCounterAction`、`NetCounterTestLocalAction`、`OwnershipTestAction` 默认值。
- 通过 UE Content Browser 普通删除三个实验 Input Action，没有使用 Force Delete。
- 更新 `Docs/FirstPerson清理与架构审计.md`，标记实验输入资产清理已完成。
- 未修改 Shooter 的移动、视角、跳跃、武器输入或运行时代码。

## 验证结果

- 通过项目内只读 MCP 检查 `BP_FirstPersonCharacter`：EventGraph 仅包含移动、视角、跳跃和触屏逻辑，没有网络实验节点。
- 对重存后的 `IMC_Default` 与 `BP_FirstPersonCharacter` 检索旧属性名和三个 Input Action 路径：无残留结果。
- 检查三个 Input Action 文件：均已不存在；对 `Content`、`Config`、`Source` 全局检索对应名称：无残留结果。
- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17834`：Build、Automation、Standalone、DedicatedNetwork、ListenNetwork、EmulatedNetwork、DisconnectCleanup 七阶段全部通过。
- Automation 结果为 1 通过、0 警告、0 失败；网络模拟使用 `PktLag=100`、`PktLoss=2`；断线清理结果为 `Orphans=0`。
- 完整验证摘要：`Saved/Automation/Runs/20260815_120557/Summary.json`。
- 验证结束后检查 UnrealEditor、ShootGame、UnrealEditor-Cmd：无残留进程。

## 遇到的问题

- 首次只点击 `BP_FirstPersonCharacter` 的 Compile 和 Save 时，蓝图没有被标记为 Dirty，因此资产没有写回磁盘，旧 C++ 属性默认值仍留在包中。
- 当前 Codex 会话没有自动挂载项目内 UE MCP 工具。
- Git 检查时提示无法读取用户级 `C:\Users\HP\.config\git\ignore`，不影响仓库差异检查和验证。

## 处理方式

- 轻微移动 EventGraph 的注释框以产生纯编辑器布局变更，再 Compile 和 Save；重写后旧属性名与资产路径全部消失，不改变蓝图运行逻辑。
- 直接连接插件提供的本机只读 MCP 端点，确认蓝图父类和 EventGraph 节点，不绕过插件的只读边界。
- 继续使用仓库内 `.gitignore` 与显式暂存文件列表；用户级 ignore 权限提示不影响本提交内容。

## 遗留项

- 根目录 `AShootGamePlayerController`、`AShootGameCameraManager`、`AShootGameGameMode` 及其兼容重定向仍等待下一阶段审计和清理。
- `AShootGameCharacter` 的最终命名与 Shooter C++ 目录整理仍需单独讨论和实施。
