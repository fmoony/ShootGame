# 移除 Horror 模板

## 日期

2026-08-14 19:33（Asia/Shanghai）

## 计划提交说明

`清理：移除 Horror 模板`

## 变更类型

- 资产清理
- C++ 清理
- 构建配置
- 项目导航

## 目的

项目后续专注于 Shooter 多人网络机制、GAS、动画分层和客户端预测学习。Horror 模板不参与这条路线，因此在已经冻结可运行基线后完整移除，减少无关资产、源码和目录噪声。

本提交只移除 Horror 玩法及处理本次发现的无引用 Redirector，不切换默认地图和默认 GameMode。

## 本提交完成内容

- 通过 Unreal Editor Content Browser 删除 `/Game/Variant_Horror` 下 15 个资产，包括 Horror 角色、GameMode、PlayerController、UI、输入、灯光效果和 `Lvl_Horror` 地图。
- 随 `Lvl_Horror` 一并删除 79 个 External Actor 与 1 个 External Object。
- 删除 `Source/ShootGame/Variant_Horror` 下 8 个 C++ 文件：Character、GameMode、PlayerController 和 UI 的 `.h/.cpp`。
- 从 `ShootGame.Build.cs` 移除 `Variant_Horror` 及其 UI 的公开 include 路径。
- 更新 `AGENTS.md` 源码目录树，使 Shooter 成为当前唯一玩法变体。
- 执行 `Fix Up Redirectors in Folder`；UE 更新 `MI_Weapon_Pistol` 的实际材质引用，并删除不再被引用的 `/Game/Weapons/Rifle/M_Weapon` Redirector。

## 验证结果

- Horror 文本引用审计：源码、构建文件、配置、项目文件及 Markdown 中均无 `Variant_Horror`、Horror 类型或 `Lvl_Horror` 残留引用。
- `Content/Variant_Horror`、Horror External Actors/Objects 和 `Source/ShootGame/Variant_Horror` 均已从工作树移除。
- `BuildEditor.ps1`：通过；UnrealHeaderTool 重新生成反射代码，`ShootGameEditor Win64 Development` 编译及链接成功。
- 完整验证命令：

  ```powershell
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17794
  ```

- 完整验证结果：`Passed`，七个阶段全部通过：
  - Build
  - Automation（Passed=1，Failed=0）
  - Standalone
  - DedicatedNetwork
  - ListenNetwork
  - EmulatedNetwork（`PktLag=100`、`PktLoss=2`）
  - DisconnectCleanup（`Orphans=0`）
- 汇总文件：`Saved/Automation/Runs/20260814_193127/Summary.json`。
- 断线清理成功标记：`ActiveWeapons=4 Orphans=0`。
- 验证完成后无残留 `UnrealEditor` 或 `ShootGame` 进程。

## 遇到的问题

- 修复 Redirector 时，UE 报告 `/Game/Weapons/Rifle/M_Weapon` 的所有引用已经成功更新，并要求选择删除或保留无引用 Redirector。该 Redirector 不在 Horror 目录中，但属于本次全项目 Redirector 整理结果。

## 处理方式

- 选择 `Delete Unreferenced Redirectors`。该操作仅删除旧路径跳转文件；实际材质引用已由 UE 重写到目标资产。清理前提交 `72997b9` 可用于恢复本次删除内容。

## 遗留项

- `DefaultEngine.ini` 仍以 `/Game/FirstPerson/Lvl_FirstPerson` 和 `BP_FirstPersonGameMode` 作为默认入口；下一提交再独立切换为 Shooter。
- FirstPerson 模板资产和根目录基础 C++ 类暂时保留，待默认入口切换并验证后再审计其引用与去留。
- 项目原有 `Plugins` 文件夹及 `McpAutomationBridge` 未参与本次清理。
