# 整理 Docs 目录：区分已完成与执行计划

- 日期：2026-08-15
- 计划提交说明：`文档：整理 Docs 目录，区分已完成与执行计划`
- 变更类型：文档

## 目的

整理 `Docs/` 目录结构，把已实现的计划文档与未实现的计划文档分目录归档，避免根目录同时混放「已执行完的方案」与「待执行的计划」，并同步更新导航与文档间相对链接。

## 本提交完成内容

- 移动已实现的计划到 `Docs/已完成计划/`：
  - `GAS接入前架构整理执行方案.md`（6 阶段 + 收尾已全部完成）
  - `多人网络改造计划.md`（服务器权威多人网络主闭环已完成）
- 移动未实现的计划到 `Docs/执行计划/`：
  - `GAS动画分层与预测扩展计划.md`（GAS 尚未接入）
- 纳入此前未跟踪的执行计划文档 `Docs/执行计划/Shooter_Content_Browser_资产目录整理执行方案.md`。
- 更新失效链接：
  - `AGENTS.md` 文档入口导航（3 处）。
  - `Docs/代码规范.md` 的 `多人网络改造计划` 引用。
  - 移动后文档内部的相对链接（`开发记录/README.md`、`FirstPerson清理与架构审计.md`、跨目录的 `../执行计划/...`、`../已完成计划/...`）。

## 验证结果

- 使用 `git mv` 移动，保留历史可追踪性。
- grep 复查移动后文档与 `AGENTS.md` 中的所有 Markdown 链接，确认指向新路径，无失效相对引用。
- 纯文档整理，无代码或资产改动，未运行自动化。

## 遇到的问题

无。

## 处理方式

无。

## 遗留项

- 历史开发记录（`Docs/开发记录/*.md`）中指向旧路径的链接未改，按规范不重写历史提交。
- `CtrlRig_FPWarp.uasset` 为用户动画修改，未纳入本提交。
