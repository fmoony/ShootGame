# MCP 编辑扩展与全量回归收尾

- 日期：2026-09-09
- 计划提交说明：`项目：整合MCP编辑扩展、武器资产与全量回归修复`
- 变更类型：编辑器插件 / 测试 / 文档 / 资产

## 目的

提交当前工作区已完成的 MCP 编辑能力扩展、配套验收脚本与文档、阶段性武器资产调整，并同步 Rifle 第一人称动画类的正式测试基线，解除 `AnimClassMapping` 对完整七阶段回归的阻塞。

## 本提交完成内容

- 扩展 `McpAutomationBridge` 的配置读取、Blueprint 默认值读取、普通 Blueprint 创建/移动/改父类、基础 K2 图编辑、编译保存、打开与截图能力。
- 增加 AnimBlueprint 受限复制能力，支持同骨架、原生 AnimInstance 父类和空目标下的图、状态机、变量及默认值复制，并补充重名嵌套图定位与动画节点参数读取。
- 新增 MCP 普通编辑和动画复制验收脚本、插件说明、变更记录以及面向外部 Agent 的完整使用契约；项目 `AGENTS.md` 增加对应导航入口。
- 保留 MCP 编辑验收使用的 `BP_McpCreatedAndMoved` 测试资产。
- 将 `ShootGame.Equipment.Presentation.AnimClassMapping` 中 Rifle 第一人称严格期望从历史 `ABP_FP_Weapon_C` 更新为当前正式配置 `ABP_FP_Rifle_C`；没有放宽 `UClass` 等值断言。
- 保存当前 Pistol 第一人称动画、角色蓝图以及 AWP/榴弹发射器武器蓝图调整。
- 纳入用户自行导入的 `SW_Fire_AWP` 与 `SW_Fire_GrenadeLauncher` 声音资产。
- 增加全量回归失败分析文档，记录旧测试基线、当前资产 CDO、历史提交和自动化中止边界的证据。

## 验证结果

- MCP 普通 Blueprint 编辑专项、配置读取、类/组件默认值读取、截图与保存检查通过；详细证据见 `2026-09-08-1728-MCP配置读取与蓝图编辑扩展.md`。
- MCP AnimBlueprint 复制专项通过：目标编译 0 Error / 0 Warning，20 张图和最终 96 个节点完成结构与连线核对；详细证据见 `2026-09-08-1755-NewAnimBlueprint第三人称动画复刻.md`。
- `Scripts/Tests/BuildEditor.ps1`：通过。
- `Scripts/Tests/RunAutomation.ps1 -TestFilter ShootGame.Equipment.Presentation.AnimClassMapping`：1 Passed / 0 Failed。
- `Scripts/Tests/RunAll.ps1 -Port 17848`：通过，报告为 `Saved/Automation/Runs/20260909_095708/Summary.json`。
- 完整七阶段均为 Passed：Build、Automation、Standalone、DedicatedNetwork、ListenNetwork、EmulatedNetwork、DisconnectCleanup。
- 全量 Automation：81 Passed / 13 Warning Passed / 0 Failed / 0 NotRun。

## 遇到的问题

- Rifle 武器当前 `FirstPersonAnimInstanceClass` 已是 `ABP_FP_Rifle_C`，但 2026-08-29 冻结测试仍严格要求 `ABP_FP_Weapon_C`，导致此前完整流程在 Automation 阶段中止。
- MCP 编辑器工具涉及配置缓存、Blueprint Schema、资产移动、重定向器、重名动画子图和热重载 DLL 等 UE 5.6 特定行为；旧工具边界不足以完成真实编辑验收。
- 自动化中的 13 个测试仍以 Warning 状态通过，主要包含无 SkeletalMesh 的轻量测试角色 socket 查询和临时测试 World 清理提示。

## 处理方式

- 先用独立 UE 进程读取 Rifle 武器 CDO，并结合 Git blob 与历史提交确认资产和 HEAD 一致，再仅同步过期的测试期望；保留严格映射检查。
- MCP 扩展沿用现有 transport，并通过工具 schema、handler、真实 HTTP 会话、资产读回、编译保存和截图形成闭环；未恢复旧的全量源码归档插件。
- 在定向测试通过后重新执行完整七阶段流程，确认后续五个此前未执行的阶段均实际运行并通过。

## 遗留项

- 全量 Automation 的 13 个 Warning 尚未在本提交中降噪；它们未造成门禁失败，但后续可单独改进测试夹具。
- MCP 测试资产继续保留在正式 Content 目录，后续若不再需要，应通过 Unreal Editor 资产系统单独审计和清理。
- 用户自行导入的两项声音资产已纳入版本控制；本提交没有额外修改其音量、并发或衰减配置，也没有宣称完成听感验收。
- Pistol 第一人称动画仍属于持续调整范围，本提交仅保存当前磁盘状态，不把其视觉效果标记为最终完成。
