# 重组 Shooter 源码目录

- 日期：2026-08-15
- 计划提交说明：`整理：重组 Shooter 源码目录`
- 变更类型：生产代码 + 资产

## 目的

执行 [GAS 接入前架构整理执行方案](GAS接入前架构整理执行方案.md) 阶段 3：把 `Source/ShootGame/Variant_Shooter/` 重组为按职责划分的 `GameFramework` / `Characters` / `AI` / `Weapons` / `UI` 目录，为 GAS 接入建立清晰的源码入口，同时保持 `/Script/ShootGame.ClassName` 不变、不增加无关类重定向。

## 本提交完成内容

- 目录重组（Git 可追踪的 `git mv`，共 32 个文件）：
  - `GameFramework/`：`ShooterGameMode`、`ShooterGameState`、`ShooterPlayerController`、`ShooterPlayerState`、`ShooterCameraManager`
  - `Characters/`：`ShooterCharacter`
  - `AI/`：`ShooterNPC`、`ShooterAIController`、`ShooterStateTreeUtility`、`EnvQueryContext_Target`
  - `Weapons/`：`ShooterWeapon`、`ShooterProjectile`、`ShooterPickup`、`ShooterWeaponHolder`
  - `UI/`：`ShooterUI`、`ShooterBulletCounterUI`
- 更新 `ShootGame.Build.cs` 的 `PublicIncludePaths`，移除 `Variant_Shooter` 相关路径，改为新目录路径。
- 更新全部 `#include "Variant_Shooter/..."` 前缀为直接文件名，依靠新的 include path 解析。
- 移除已清空的 `Source/ShootGame/Variant_Shooter/` 目录。
- 修复阶段 2 遗留的动画蓝图资产：`ABP_FP_Weapon`、`ABP_FP_Pistol` 中 `Cast To ShooterCharacter` 节点引脚与 `FirstPersonMesh`/`FirstPersonCameraComponent` 变量引用在 `ShooterCharacter` 继承链变化后失效，已在编辑器重新编译修复。

## 验证结果

- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\BuildEditor.ps1`，全部 18 个源码文件重编译通过。
- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17794`，七阶段（Build / Automation / Standalone / DedicatedNetwork / ListenNetwork / EmulatedNetwork / DisconnectCleanup）全部 Passed。
- 实际结果文件：`Saved/Automation/Runs/20260815_142852/Summary.json`，顶层 `status: Passed`、`error: null`。

## 遇到的问题

- 首次完整验证在 `DedicatedNetwork` 阶段失败（仅 1/2 成功标记）。日志定位到动画蓝图 `ABP_FP_Weapon`/`ABP_FP_Pistol` 编译错误：`不能连接引脚 Target 和 AsShooter Character`、`未找到 FirstPersonCameraComponent / FirstPersonMesh 属性`。经查该错误在阶段 2 的 Network 日志中已存在，属阶段 2 `ShooterCharacter` 从「继承 `ShootGameCharacter`」改为「直接继承 `ACharacter`」后动画蓝图 Cast 节点与属性引用失效的遗留问题，本阶段才暴露。

## 处理方式

- 确认该错误与目录重组无关（C++ 编译干净、Automation 与 Standalone 均通过），在编辑器内修复两个动画蓝图的 Cast 节点与变量引用并重新编译保存，随后完整验证全部通过。

## 遗留项

- `Variant_Horror/UI/` 为模板遗留目录，方案未纳入本阶段范围，保持不动。
- `DefaultEngine.ini` 中 `TP_FirstPerson*` → `ShootGame*` 重定向仍待阶段 4 统一收束。
