# C1 建立 ShooterAimIK 节点基础设施并完成 C0 审计

- 日期：2026-08-23
- 计划提交说明：`表现：建立ShooterAimIK节点基础设施并归档C0审计`
- 变更类型：生产代码 / 构建 / 文档

## 目的

《第三人称C++程序化瞄准IK实施计划》首次执行：完成 C0 基线审计（冻结当前实验状态），并建立 C1 Runtime AnimNode + Editor GraphNode 基础设施，使 `Shooter Aim IK` 节点可在 `ABP_TP_Rifle` 中作为 Skeletal Control 使用（当前为空壳：Pose 完全透传、不修改骨骼）。C1 自动验证完成后按计划进入人工 Asset Gate。

## 本提交完成内容

### C0 审计结论（只读 Unreal MCP 复查，未改动任何 .uasset）

- `ABP_TP_Rifle` AnimGraph 当前 Pose 链：`Output Pose ← Layered blend per bone ← (BasePose: LocomotionStateMachine | BlendPoses_0: Slot Arms ← AO_Rifle ← MF_Rifle_Idle_ADS)`；`AO.Y ← PitchN`（X 未接）；**实验 Control Rig 节点（AnimGraphNode_ControlRig_0）孤立存在于图中，Source/Pose 均未接线，不影响最终 Pose**，其输入来自 `AimPoint` / `HandToMuzzle` 变量。
- `ABP_TP_Rifle` EventGraph 已含 `Event Blueprint Update Animation → Cast To ShooterCharacter → GetAimPitchN → Set PitchN` 契约链（用户最近保存），并附带 `Print String`（每帧调试打印）、`Aim offset_Yaw` 计算、`AimPoint`/`HandToMuzzle` Socket 相对变换实验链。
- `ABP_TP_Pistol` AnimGraph 为干净基线（`AO.Y ← PitchN`，无实验节点）。
- 未提交实验资产：`Ctrl_TP_Aim_Rifle`、`Ctrl_TP_Aim_Pistol`（ControlRigBlueprint）、`MM_Rifle_Idle_ADS_AO_CDH`、`MM_Rifle_Idle_ADS_AO_CUH`（手工 AO Sample）；已修改：`AO_Rifle`、`MM_Rifle_Idle_ADS_AO_CC`、两个 AnimBP、`BP_ShooterCharacter`、`ShooterCharacter.h`（含未提交的 `GetPresentationAimTargetBP`）。全部保持未提交，等待用户决定。

### C1 基础设施

- 新增 Runtime：`Source/ShootGame/Animation/AnimNodes/AnimNode_ShooterAimIK.h/.cpp`
  - `FAnimNode_ShooterAimIK : FAnimNode_SkeletalControlBase`；`HandBone`（默认 `hand_r`）、`AimDirectionWorld`、`HandToMuzzle`、`MaxCorrectionAngle` 输入属性已就位（C2 生效）。
  - C1 空壳：`EvaluateSkeletalControl_AnyThread` 空实现（Pose 完全透传）；`IsValidToEvaluate` / `InitializeBoneReferences` 基于 `HandBone`。
- 新增 Editor 模块 `Source/ShootGameEditor/`：`ShootGameEditor.Build.cs`、`ShootGameEditor.h/.cpp`（模块入口）、`Animation/AnimGraphNode_ShooterAimIK.h/.cpp`（`UAnimGraphNode_ShooterAimIK : UAnimGraphNode_SkeletalControlBase`，标题 "Shooter Aim IK"，分类 `Animation|Skeletal Controls`）。
- 修改：`Source/ShootGame/ShootGame.Build.cs`（Public 依赖加 `AnimGraphRuntime`）、`Source/ShootGameEditor.Target.cs`（ExtraModuleNames 加 `ShootGameEditor`）、`ShootGame.uproject`（Modules 加 Editor 模块）。
- 新增本开发记录。

## 验证结果

- `Scripts/Development/RefreshVisualStudioFiles.ps1`：Succeeded。
- `Scripts/Tests/BuildEditor.ps1`（ShootGameEditor Win64 Development）：通过；`ShootGame` 与 `ShootGameEditor` 模块均编译链接成功。
- Game Target 验证：`Build.bat ShootGame Win64 Development` 通过，确认 `ShootGameEditor` 不进入 Game 构建（计划 C1 项 3）。
- Focused Automation：`ShootGame.Aim.Math` 1/1 Success（`Saved/Automation/Reports/20260823_111559_ShootGame.Aim.Math`）。
- Editor-only 依赖泄漏检查：Runtime 模块仅新增 `AnimGraphRuntime`（Runtime 模块）；`ShootGameEditor` 仅挂载于 Editor Target。无泄漏。

## 遇到的问题

- 首次 BuildEditor 失败（exit 6）：`AnimGraphNode_ShooterAimIK.cpp` 与 `Module.ShootGameEditor.gen.cpp` 均报 `C1083` 找不到头文件——`ShootGameEditor` 模块未声明对 `ShootGame` 的依赖，且跨模块 include 需要从项目 Source 根写起（项目惯例为 `Tests/Network/...` 这类前缀）。

## 处理方式

- `ShootGameEditor.Build.cs` PublicDependencyModuleNames 增加 `ShootGame`；
- include 改为 `"ShootGame/Animation/AnimNodes/AnimNode_ShooterAimIK.h"`、`"ShootGameEditor/Animation/AnimGraphNode_ShooterAimIK.h"`；
- 重建通过。

## 遗留项

- **C1 人工 Asset Gate（下一步必须停止交给用户）**：在 `ABP_TP_Rifle` AnimGraph 中搜索并插入 `Shooter Aim IK` 到最终 Pose 链（建议插在 `Layered blend per bone` 之后、`Output Pose` 之前，或 `Slot Arms` 之后），Alpha 先保持 0；Compile + Save。若节点无法搜索，用户反馈后排查 Editor Module / GraphNode 注册。
- C0 待用户确认：实验 Control Rig 节点（Rifle AnimGraph 内孤立节点）保留（Alpha=0）或人工删除；黄色/蓝色 Debug 线是否仍可观察。
- 未提交的用户资产（实验 Control Rig / AO Sample / AnimBP / BP_ShooterCharacter / ShooterCharacter.h）等待用户意图；本提交不含任何 .uasset。
- C2 将在用户完成 Asset Gate 后实现单骨骼几何闭环与 Math Feature Tests。
