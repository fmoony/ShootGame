# 建立网络射击 AI 自主验证契约

- 日期：2026-09-16
- 计划提交说明：`文档：建立网络射击AI自主验证契约`
- 变更类型：文档

## 目的

将网络射击的验收责任从用户逐项理解 Coordinator 内部 Phase / Case，调整为用户定义顶层不变量、
Agent 自主选择和维护验证链。避免测试内部编号、恒真断言、假 0 或未执行分支被误报为系统级通过。

## 本提交完成内容

- 新增 `Docs/架构/网络射击AI自主验证契约.md`，冻结五条网络射击不变量、证据质量规则、测试分层、
  自主验证流程、升级边界和固定报告格式。
- 在 P1 执行计划与详细实施方案中声明该契约是最终验收和报告的首要依据。
- 将 P1 最终验收改为五条不变量入口，并明确 P1-D 的 RPC、门闩和 Phase 只是可替换的测试实现细节。
- 在项目导航与协作约定中加入契约入口，确保后续 Agent 在网络射击改动前读取并执行。
- 未修改生产代码、测试代码、资产或脚本。

## 验证结果

- 运行 `Scripts/Development/CheckTextLayout.ps1 -Scope WorkingTree`：通过，0 条警告。
- 追加运行 `-Scope All` 检查未跟踪文档；全仓历史文件仍有既有告警，本轮新增的契约和开发记录
  已无版式命中。
- 运行 `git diff --check`：通过。
- 本轮只有 Markdown 文档变化，不运行 Unreal Build、Automation 或网络会话。

## 遇到的问题

无。

## 处理方式

无。

## 遗留项

- Coordinator 当前已有的恒真断言、假 0、重复场景和内部编号尚未在本提交中整理；本提交只建立后续
  治理与验收契约。
- 当前工作区原有的 Coordinator 未提交改动不属于本提交范围。
