# 大阶段 A 收口七阶段回归通过

- 日期：2026-09-11
- 计划提交说明：`回归：大阶段A收口七阶段完整回归通过`
- 变更类型：文档

## 目的

《武器与 Inventory 正式架构实施计划》大阶段 A（A1 Definition 解析闭环、A2 Inventory 切换、A3 FireBehavior、A4 资产迁移与旧路径退出）全部完成后，按计划第 6 节运行一次七阶段完整回归并归档证据，宣告大阶段 A 收口、允许进入大阶段 B。

## 本提交完成内容

- 归档大阶段 A 收口回归证据与门槛核对结论（纯文档提交，无代码改动）。

## 验证结果

- 完整入口：`RunAll.ps1 -Port 17796`；Summary：`Saved/Automation/Runs/20260911_125014/Summary.json`，顶层 `status=Passed`。
- 七个阶段全部 Passed：Build 26.6s / Automation 19.8s / Standalone 11.8s / DedicatedNetwork 36.4s / ListenNetwork 29.6s / EmulatedNetwork 40.9s / DisconnectCleanup 47.3s。
- 会话日志核对（`Saved/Automation/Network/`）：
  - 125113（Dedicated）：2 条 `AUTOMATION_TEST_CLIENT_SUCCESS`；
  - 125149（Listen）：1 条成功标记；
  - 125219（Emulated）：2 条成功标记，`PktLag set to 100` ×2、`PktLoss set to 2` ×2；
  - 125300 / 125323（DisconnectCleanup 两次会话）：`AUTOMATION_TEST_DISCONNECT_SUCCESS` ×1、`Orphans=0` ×2；
  - 所有日志 `AUTOMATION_TEST_FAILURE` = 0。
- 门槛核对（计划 6.A 收口）：
  - 生产代码不存在"由 WeaponClass 名伪造 DefinitionId"的路径（`MakeDefinitionIdForWeaponClass` 零命中）；
  - Inventory 不从 Weapon CDO 初始化 Ammo（授予与换弹容量均 Definition 优先）；
  - Projectile 只从 Projectile FireBehavior 的服务器路径生成（玩家正式链路；NPC 兼容路径为已记录 PvE 遗留）；
  - Fire / Reload / Equip / Pickup / HUD 网络闭环无回退（上述网络会话全链路通过）。

## 遇到的问题

- 无。

## 处理方式

- 无。

## 遗留项

- Cook 真实打包验证不在七阶段矩阵内（见 A1/A4 记录的证据边界）。
- WeaponActor CDO 表现配置消费与 NPC 兼容路径的收口在 B 阶段处理。
