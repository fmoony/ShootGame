# 迁移 Shooter 非地图资源

- 日期：2026-08-16
- 计划提交说明：`资产：迁移 Shooter 非地图资源`
- 变更类型：资产 + 生产代码

## 目的

执行 [Shooter Content Browser 资产目录整理执行方案](../执行计划/Shooter_Content_Browser_资产目录整理执行方案.md) 阶段 3：把 Shooter 非地图资产从 `/Game/Variant_Shooter` 迁移到按资产类型组织的 `/Game/Shooter` 正式目录，并同步所有硬编码文本路径。

## 本提交完成内容

- 41 个非地图资产迁入 `/Game/Shooter/`：
  - `Animation/`：FirstPerson（ABP_FP_*、ABP_FP_Copy、FP_Rifle_Shoot_Montage）、ThirdPerson（ABP_TP_*）、Rigs（CtrlRig_FPWarp、Ctrl_HandAdjusment、Ctrl_HandAdjusment_Pistol）
  - `Blueprints/`：Framework（GameMode、PlayerController）、Characters（BP_ShooterCharacter、BP_FirstPersonCharacter）、AI（6 个）、Weapons（10 个）
  - `Data/`（DT_WeaponData）、`Input/`（5 个）、`UI/`（3 个）、`FX/`（NS_MuzzleFlash_Simple）、`Materials/`（M_Explosion）、`Meshes/`（SM_FoamBullet）
- 同步 8 处硬编码文本路径：
  - `ShooterPlayerController.cpp`（UI_Shooter）
  - `ShooterNetworkTestCoordinator.cpp`（武器 Rifle/Pistol）
  - `ShooterWeaponAutomationTests.cpp`（ABP_TP_Rifle/Pistol + 武器）
  - `DefaultEngine.ini`（GlobalDefaultGameMode → `/Game/Shooter/Blueprints/Framework/BP_ShooterGameMode`）
- 修复迁移过程中暴露的引用断链：
  - `BP_FirstPersonCharacter` → `ABP_FP_Copy`（旧路径，第一人称 Copy Pose 支撑）
  - `BP_ShooterProjectile_Bullet` → `SM_FoamBullet`（中间过渡路径）
  - `BP_ShooterGrenadeExplosion` → `M_Explosion`（中间过渡路径）
  - `BP_ShooterGrenadeExplosion` 开启复制（专用服务器场景下客户端可见爆炸球体）
  - 第一人称手部朝向调整失效（武器 Mesh 重复挂角色 AnimBP 导致姿态二次计算，已在 `/Game/Characters` 下修正 AO_Pistol / SKM_Manny_Simple）

## 验证结果

- 执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17794`，七阶段（Build / Automation / Standalone / DedicatedNetwork / ListenNetwork / EmulatedNetwork / DisconnectCleanup）全部 Passed。
- 实际结果文件：`Saved/Automation/Runs/20260816_152656/Summary.json`，顶层 `status: Passed`、`error: null`。
- 全量扫描 `Content/Shooter`：`/Game/Variant_Shooter` 残留为 0、中间过渡路径残留为 0（仅 `SM_FoamBullet` 内 1 处 `/Game/Variants/...SK_Pistol` 历史废弃 import，不影响显示）。

## 遇到的问题

- 首次完整验证在 Automation 阶段失败：武器蓝图 import 表仍指向旧路径 AnimBP/Montage/FX。
- 迁移后多个软引用/硬引用断链（动画、子弹 Mesh、爆炸材质），以及榴弹爆炸球体在专用服务器客户端不可见。

## 处理方式

- 逐一定位断链资产（文件系统扫描 + MCP 蓝图检查 + 迁移前后对比），在编辑器重新指定引用并 Compile + Save。
- 爆炸球体问题定位为网络复制：纯表现 Actor 未开启复制时客户端不可见，开启复制后解决。

## 遗留项

- 地图 `Lvl_Shooter`、`Lvl_Test` 仍留在 `/Game/Variant_Shooter`，留待阶段 4 单独迁移。
- 地图相关硬编码路径（`DefaultEngine.ini` 的 EditorStartupMap/GameDefaultMap/ServerDefaultMap、`DefaultEditor.ini` 的 SimpleMapName、脚本 MapPath）暂未改，待地图迁移后同步。
