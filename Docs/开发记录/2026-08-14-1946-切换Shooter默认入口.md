# 切换 Shooter 默认入口

## 日期

2026-08-14 19:46（Asia/Shanghai）

## 计划提交说明

`配置：切换 Shooter 默认入口`

## 变更类型

- 项目配置
- 默认地图与 GameMode

## 目的

项目已经移除 Horror 模板，后续开发也以 Shooter 多人玩法为主。将编辑器、普通游戏和服务器的默认入口统一到 Shooter，可避免无参数启动时仍进入旧的 FirstPerson 示例地图，并为后续审计 FirstPerson 模板资产建立明确边界。

本提交只切换入口配置，不删除 FirstPerson 资产和根目录基础 C++ 类。

## 本提交完成内容

- 将 `EditorStartupMap` 切换为 `/Game/Variant_Shooter/Lvl_Shooter.Lvl_Shooter`。
- 将 `GameDefaultMap` 切换为 `/Game/Variant_Shooter/Lvl_Shooter.Lvl_Shooter`。
- 将 `ServerDefaultMap` 从引擎空白入口切换为 `/Game/Variant_Shooter/Lvl_Shooter.Lvl_Shooter`。
- 将 `GlobalDefaultGameMode` 切换为 `/Game/Variant_Shooter/Blueprints/BP_ShooterGameMode.BP_ShooterGameMode_C`。

## 验证结果

- 无参数 Editor-Cmd 启动日志确认加载 `Content/Variant_Shooter/Lvl_Shooter.umap`。
- Shooter 地图启动时 Map Check 为 `0 Error(s), 0 Warning(s)`。
- 完整验证命令：

  ```powershell
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Scripts\Tests\RunAll.ps1 -Port 17794
  ```

- 完整验证结果：`Passed`，七个阶段全部通过：
  - Build
  - Automation（Passed=1，Failed=0）
  - Standalone
  - DedicatedNetwork（2 个客户端）
  - ListenNetwork（1 个远程客户端）
  - EmulatedNetwork（2 个客户端，`PktLag=100`、`PktLoss=2`）
  - DisconnectCleanup（`ActiveWeapons=4 Orphans=0`）
- Dedicated、Listen 和 EmulatedNetwork 均完成武器切换、弹药归属可见性、开火、伤害、死亡、复活、队伍计分、远端 Pitch 与 Montage 校验。
- 汇总文件：`Saved/Automation/Runs/20260814_194349/Summary.json`。
- 验证完成后无残留 `UnrealEditor`、`UnrealEditor-Cmd` 或 `ShootGame` 进程。

## 遇到的问题

- 第一次尝试切换入口并验证时，DedicatedNetwork 暴露了原有近距离目标点可能位于枪口后方的问题，弹道与服务器控制方向的点积曾为负数。

## 处理方式

- 当时先回退入口配置，将生产射击逻辑修复作为独立提交 `48cfad2` 完成并通过七阶段验证。
- 本提交随后只重新应用入口配置，并从干净工作区再次执行完整验证，确保配置切换本身可独立回退和审查。

## 遗留项

- `DefaultInput.ini` 中仍有 FirstPerson 移动端触控界面引用。
- `DefaultEditor.ini`、`DefaultEditorPerProjectUserSettings.ini` 与历史类重定向中仍可能存在 FirstPerson 路径；下一步统一审计。
- `/Game/FirstPerson` 资产及根目录 `ShootGameCharacter`、`ShootGamePlayerController`、`ShootGameGameMode` 等基础类尚未判断去留，本提交不做删除。
