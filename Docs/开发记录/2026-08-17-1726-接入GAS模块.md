# 接入 GAS 模块

- 日期：2026-08-17
- 计划提交说明：`GAS：接入 Gameplay Ability System 模块`
- 变更类型：构建

## 目的

执行 GAS 第一阶段执行计划子阶段 1A：只让工程具备编译和加载 Gameplay Ability System 的能力。不创建 ASC、AttributeSet、GameplayAbility、GameplayEffect 或任何 GAS 业务逻辑，也不改变现有射击玩法。1A 只证明 GAS 依赖变化不会破坏现有工程。

## 本提交完成内容

- `ShootGame.uproject`：Plugins 新增 `GameplayAbilities`（Enabled: true）。`GameplayTags` 与 `GameplayTasks` 是该插件内的模块，不需要独立插件条目。
- `Source/ShootGame/ShootGame.Build.cs`：`PublicDependencyModuleNames` 新增 `GameplayAbilities`、`GameplayTags`、`GameplayTasks`（公开依赖，后续 ASC 组件类型会出现在公开头文件中）。
- 未创建任何 ASC、AttributeSet、GA/GE/GameplayCue 或 GAS Input，未修改任何生产类。

## 验证结果

- 前置门禁（本子阶段开始前）：
  - 七阶段自动化基线通过：`Saved/Automation/Runs/20260817_170605/Summary.json`，顶层 Passed，七阶段全 Passed；Dedicated/Listen/Emulated 成功标记 2/1/2，Emulated 日志含 `PktLag set to 100` 与 `PktLoss set to 2`，Disconnect 输出 `AUTOMATION_TEST_DISCONNECT_SUCCESS` 且 `Orphans=0`，无失败标记，测试进程零残留。
  - 迁移后 Cook 检查通过：`UnrealEditor-Cmd -run=Cook -TargetPlatform=Windows`，597 个包烹饪成功，`Success - 0 error(s), 0 warning(s)`，退出码 0。
  - `git diff --check` 通过；`Source/Config/Scripts` 无 `/Game/Variant_Shooter` 引用。
- Focused Validation：
  - Editor 目标编译（`ShootGameEditor Win64 Development`）：Succeeded，34.80 秒（.uproject 变化触发 makefile 重建与模块重编译）。
  - Game 目标编译（`ShootGame Win64 Development`）：Succeeded，76.03 秒（首次建立 Game 目标）。
  - 项目没有独立 Server Target（仅 `ShootGame` / `ShootGameEditor`），网络测试沿用 `UnrealEditor-Cmd -server` 模式，无需额外验证。
  - 模块可解析、头文件正常、插件加载正常（启动日志出现 `LogGameplayAbilityAudit`），无 Module Not Found、无链接错误。
- Full Regression（`RunAll.ps1 -Port 17777`）：
  - `Saved/Automation/Runs/20260817_171753/Summary.json`，顶层 Passed；Build 0.95s / Automation 56.63s（`ShootGame.Weapon.Configuration` 1 通过 0 失败）/ Standalone / DedicatedNetwork / ListenNetwork / EmulatedNetwork / DisconnectCleanup 全部 Passed。
  - 成功标记：Dedicated 2、Listen 1、Emulated 2；Emulated 服务器与客户端日志均出现 `PktLag set to 100` / `PktLoss set to 2`；Disconnect 输出 `AUTOMATION_TEST_DISCONNECT_SUCCESS`；所有日志无 `AUTOMATION_TEST_FAILURE`；测试启动的 `UnrealEditor-Cmd.exe` 全部回收。
- 编译时间变化：接入 GAS 后未见异常编译时间变化；回归中的 Build 阶段为 0.95 秒增量编译。

## 遇到的问题

- 第一次以 pwsh 直接调用 `.\Scripts\Tests\BuildEditor.ps1` 被本机执行策略拒绝（`running scripts is disabled on this system`），编译未执行。

## 处理方式

- 属于执行环境问题而非代码错误。按项目 SOP 改用文档规定的 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File` 入口重试，两个目标编译与后续回归均通过。

## 遗留项

- `GameplayTags` / `GameplayTasks` 目前只有模块依赖，尚未注册任何原生 GameplayTag；待 1C 使用 SetByCaller 数值标签时再评估。
- 工作区存在用户未提交的输入资产重构（`IA_Shoot`/`IA_SwapWeapon` 删除、`IMC_Weapons` 修改等），本提交未触碰、未暂存，保持原状。
- ASC、AttributeSet 与 GameplayEffect 逻辑属于后续 1B/1C 子阶段，不在本提交范围内。
