# 归档解耦重构执行计划

- 日期：2026-08-26
- 计划提交说明：`架构：归档解耦重构计划并更新导航`
- 变更类型：文档归档 + 导航更新

## 目的

《Shooter 核心玩法架构解耦重构执行计划》R0～R8 已全部实施、提交并通过回归。按 R8 完成条件，把计划文档从 `Docs/执行计划` 移入 `Docs/已完成计划`，并更新 AGENTS 导航指向归档位置。

## 本提交完成内容

- `Docs/执行计划/Shooter核心玩法架构解耦重构执行计划.md` → `Docs/已完成计划/`。
- 计划文档状态行更新为“已完成（2026-08-26）”，并说明遗留项与人工视觉验收记录位置。
- `AGENTS.md` 导航中“解耦重构计划”从当前架构阶段入口改为已完成归档入口。

## 验证结果

- 本次只改文档，无源码/资产变更，不需要重新编译。
- 已确认本提交只 stage AGENTS.md 与归档后的计划文档；用户工作区其他未提交改动保持原样。

## 遇到的问题

- AGENTS.md 与计划文件原先是用户未提交改动；经用户明确授权后，本次只提交导航行与计划归档，不包含其他未提交文档改动。

## 处理方式

- 归档提交独立于 R8 代码提交，便于单独回退导航/归档决策。

## 遗留项

- 第一/第三人称最终人工视觉验收。
- R6.3 旧 AnimBP EventGraph 冗余节点与旧变量的后续清理。
- `GetPresentationAimTargetBP` / `GetAimPitchN` 两个 BP 兼容契约在旧图节点清理后的退出评估。
