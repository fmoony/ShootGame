# 统一 P1 涉及文件换行布局

- 日期：2026-09-12
- 计划提交说明：`格式：统一P1涉及文件的换行布局`
- 变更类型：纯格式整理

## 目的

在 P1 开始前先收敛其预计涉及文件的换行与空白布局，避免后续功能提交混入历史格式噪声。
本提交不实现 LocalPredicted 射击反馈，也不改变现有网络、Ability、武器或测试行为。

## 本提交完成内容

- 对 P1 计划列出的 11 个既有文件执行全范围布局核对；
- 整理其中 6 个存在超宽行或行尾空白的文件；
- 将长函数声明、调用参数、条件表达式和日志格式串按 120 列上限拆分；
- 保持日志文本、格式参数顺序和运行时表达式不变；
- 其余 5 个原本符合规则的文件不制造无意义差异。

## 验证结果

- P1 涉及文件全范围布局检查：Passed；
- `git diff --check`：Passed；
- `Scripts/Tests/BuildEditor.ps1`：Passed；
- 未运行 Automation 或七阶段完整回归：本提交没有功能改动，按大阶段验证节奏只做编译确认。

## 遇到的问题

- Unreal Editor 仍占用常规模块，构建生成分块模块 DLL；UBT 编译与链接均成功；
- 构建继续报告既有警告：`Source/ShootGame/Pool` include 目录不存在，本提交未扩大范围处理。

## 遗留项

- P1 仍处于未开始状态；本提交只提供干净的格式基线。
